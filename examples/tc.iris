fn fib(n: Int) {
    if (n < 2) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}

val arr: Array[Int] = [1, 2, 3, 4];

for (val x : arr) {
    print(x);
}

print(fib(10));


fn add(x: Int, y: Int): Int {
    return x + y;
}

val x: Int = add(2, 2);

