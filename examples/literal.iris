// String
val str = "Hello, world";

// char
val char = 'c';

// array
val arr = ['h', 'e', 'l', 'l', 'o'];

// integers
val f: Int = 500 * 3;

// float
val float = 5.50;

// object
val obj = {
    foo:"foo",
    bar: "bar"
};

if (true) {
   print("Hello");
} else {
  print("Oops");
}
